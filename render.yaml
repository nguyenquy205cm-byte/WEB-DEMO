staticSites:
  - name: sneaker-store-frontend
    runtime: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
